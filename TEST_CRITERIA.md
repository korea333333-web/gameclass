# GameClass 테스트 기준

> 각 스프린트의 완료 판정 기준 모음.
> Sprint 1만 우선 작성. Sprint 2~5는 해당 스프린트 시작 전에 추가.

---

## 작성 규칙

- **검증 가능**: "잘 동작한다" ❌ → "10MB 파일 3초 이내 처리" ✅
- **구체적**: "에러 처리" ❌ → "잘못된 이메일 입력 시 'kw.ac.kr 이메일만 가능' 메시지 표시" ✅
- **자동화 가능**: pytest/jest/Playwright로 테스트 코드를 쓸 수 있는 형태

---

## Sprint 1 완료 기준 (인프라 + 인증)

### TC-1.1: 프로젝트 인프라

- [ ] Next.js 15 (App Router) 프로젝트 정상 빌드 (`npm run build` 에러 0)
- [ ] TypeScript 컴파일 에러 0개 (`tsc --noEmit` 통과)
- [ ] ESLint 경고 0개 (`npm run lint` 통과)
- [ ] TailwindCSS + shadcn/ui 설치 후 버튼 컴포넌트 정상 렌더링
- [ ] Vercel에 배포되어 공개 URL로 접속 가능 (예: gameclass.vercel.app)
- [ ] 환경변수 (`SUPABASE_URL`, `SUPABASE_ANON_KEY`) 정상 로드

### TC-1.2: 인증 (Auth) — 학번+비밀번호 방식 (2026-04-27 변경)

#### 가입(비번 생성) — `/signup`

- [ ] 사전 등록 명단(`roster`)에 없는 학번/이름 조합 입력 시 "등록되지 않은 학생입니다" 에러
- [ ] 명단에 있는 학번+이름 입력 → 비밀번호 설정 화면으로 진행
- [ ] 비밀번호 정책: 8자 이상, 영문자 1개 + 숫자 1개 이상 포함
- [ ] 비밀번호 확인 입력 불일치 시 "비밀번호가 일치하지 않습니다" 에러
- [ ] 가입 성공 → 자동 로그인 → 홈(`/`)으로 진입
- [ ] 이미 가입된 학번으로 다시 가입 시도 시 "이미 가입된 학번입니다 — 로그인 화면으로 이동" 안내

#### 로그인 — `/login`

- [ ] 학번 + 비밀번호 정상 입력 시 로그인 성공
- [ ] 잘못된 비번/학번 입력 시 "학번 또는 비밀번호가 일치하지 않습니다" 에러 (학번 존재 여부 노출 ❌)
- [ ] 로그인 상태에서 페이지 새로고침해도 세션 유지 (Supabase 기본 30일)
- [ ] 로그아웃 클릭 시 로그인 페이지로 리다이렉트
- [ ] 로그아웃 상태에서 `/`(홈), `/profile` 접근 시 `/login`으로 리다이렉트

#### 어드민 분리 — `/admin/login`

- [ ] 어드민 로그인 통로(`/admin/login`)는 학생 로그인과 별도 (이메일+비번)
- [ ] 학생 계정으로 `/admin/*` 경로 접근 시 차단됨 → `/`로 리다이렉트
- [ ] 어드민 로그인 후 `/admin/roster`(명단 관리), `/admin/reset`(비번 리셋) 접근 가능

### TC-1.3: 프로필

- [ ] 첫 로그인 후 프로필 미작성 시 `/profile`로 자동 리다이렉트
- [ ] 프로필 폼: 학번(8자리 숫자), 이름(2~10자), 학년(1~4) 입력 가능
- [ ] 학번 형식 검증: 8자리 숫자 아니면 "학번은 8자리 숫자여야 합니다" 에러
- [ ] 저장 클릭 시 Supabase `profiles` 테이블에 데이터 저장
- [ ] 새로고침해도 입력한 값 유지 (`profiles` 테이블에서 자동 로드)
- [ ] 프로필 작성 완료 후 `/`(홈) 접근 시 정상 진입

### TC-1.4: 라우팅 + 네비게이션

- [ ] 하단 탭 네비게이션 표시: [홈] [시간표] [과제] [팀] [프로필]
- [ ] 각 탭 클릭 시 해당 페이지로 이동 (Sprint 1에서는 페이지 자체는 빈 상태 OK)
- [ ] 현재 활성 탭 시각적으로 구분 (네온 컬러 강조)
- [ ] 모바일(375px)에서 하단 탭 정상 표시
- [ ] 데스크톱(1280px+)에서는 좌측 사이드바로 자동 전환

### TC-1.5: PWA 기본

- [ ] `public/manifest.json` 정상 로드 (Chrome DevTools Application 탭에서 확인)
- [ ] PWA 아이콘 192x192, 512x512 두 종류 등록
- [ ] Service Worker 등록 성공 (Application 탭에서 확인)
- [ ] 모바일 Chrome에서 "홈 화면에 추가" 프롬프트 표시
- [ ] Lighthouse PWA 점수 **80점 이상** (Sprint 5에서 90+ 목표)

### TC-1.6: 디자인 기본 (impeccable-design 원칙)

- [ ] 다크 모드 컬러 시스템 적용:
  - 배경 `#0A0E1A`
  - 카드 `#131825`
  - 강조 `#00F0FF` (시안 네온)
  - 경고 `#FF3D5C`
  - 성공 `#ADFF2F`
- [ ] **AI 슬롭 회피 검증** (코드 검색):
  - 보라 그라디언트 (`from-purple-`, `to-purple-`) 사용 0개
  - 무지성 흰색 카드 + 파란 CTA 0개
  - 의미 없는 그림자 (`shadow-2xl` 등) 남발 없음
- [ ] Pretendard 폰트 정상 로드 (한글 굵기 일관성)
- [ ] JetBrains Mono 폰트 정상 로드 (숫자 표시용)
- [ ] 다크 모드 토글 동작 (다크가 기본, 라이트는 옵션)
- [ ] 반응형 동작 확인:
  - 모바일 375px (iPhone SE)
  - 태블릿 768px (iPad)
  - 데스크톱 1280px+
  - 어느 화면에서도 가로 스크롤 발생 ❌

### TC-1.7: 성능

- [ ] 첫 화면 로딩 (FCP) < **2초** (Lighthouse 측정)
- [ ] 페이지 이동 (라우팅) < **200ms** (체감)
- [ ] Time to Interactive (TTI) < **3초**
- [ ] 빌드 후 번들 크기 < **500KB** (gzip 기준)

### TC-1.8: 데이터 보안

- [ ] Supabase RLS (Row Level Security) 활성화
- [ ] 다른 사용자의 `profiles` 데이터 조회 불가 (본인 것만)
- [ ] 환경변수 클라이언트 노출 검증 (`NEXT_PUBLIC_` 접두사 없는 비밀키 노출 0)

---

## Sprint 1 데모 시나리오

다음 시나리오가 처음부터 끝까지 막힘 없이 완주되어야 Sprint 1 완료로 판정:

```
1. (사전) 어드민이 /admin/roster에서 명단 등록 (학번/이름)
2. 새 시크릿 창에서 https://gameclass.vercel.app 접속
3. 로그인 페이지 표시 → "처음이세요? 비밀번호 만들기" 클릭
4. 학번 "9999999" + 이름 "외부인" 입력
   → "등록되지 않은 학생입니다" 에러 확인 ✅
5. 학번 "2026038001" + 이름 "최정운" 입력 → 비밀번호 설정 화면
6. 비밀번호 "abc12345" 두 번 입력 → 가입 완료, 자동 로그인
7. 학년 입력 화면(/profile) → "1" 선택 후 저장
8. 홈 화면(/) 진입 → 빈 대시보드 표시
9. 하단 탭 클릭하며 5개 페이지 이동 가능 확인
10. 로그아웃 후 /login에서 학번+비번 재로그인 → 정상 진입
11. 모바일 Chrome에서 "홈 화면에 추가" → 앱처럼 실행 가능 확인
```

---

## Sprint 2~5 완료 기준

> 해당 스프린트 시작 전에 이 문서에 추가합니다.
> Generator(코드 작성 AI)가 코드 작성 시 반드시 참고하도록 합니다.

### Sprint 2: 시간표 + 과제 To-do (2026-04-27 작성)

#### TC-2.1: 시간표 그리드 뷰

- [ ] `/schedule` 페이지에 **월~금 가로 / 9시~22시 세로** 그리드 표시
- [ ] 30분 단위로 행 분할 (9:00, 9:30, 10:00 ...)
- [ ] 등록된 과목은 해당 요일/시간 셀에 **카드 형태로 표시**
  - 카드 안에 과목명 + 강의실(있으면)
  - 시간 길이만큼 셀 높이 자동 계산 (예: 90분 수업이면 3칸 차지)
- [ ] 카드 클릭 시 상세(과목명/요일/시간/강의실/교수) 표시 + 수정/삭제 버튼
- [ ] 빈 셀 클릭 시 그 시간으로 새 과목 추가 폼 자동 채워서 열림
- [ ] 모바일(375px) — 가로 스크롤 또는 일별 뷰로 전환
- [ ] 디자인 시스템 유지 (Warm Paper, Phosphor Icons, AI 슬롭 0)

#### TC-2.2: 과목 추가/수정/삭제

- [ ] "과목 추가" 버튼 클릭 → 폼 모달 또는 페이지 열림
- [ ] 입력 필드: **과목명**(필수, 1~30자), **요일**(월~금 선택), **시작 시간**, **종료 시간**, **강의실**(선택), **교수**(선택), **색상**(7가지 차분한 톤 중 선택)
- [ ] 시작 시간 ≥ 종료 시간 검증
- [ ] 같은 요일에 시간 중복 시 "이 시간에 이미 다른 수업이 있습니다" 경고 (저장은 허용)
- [ ] 저장 성공 시 그리드 즉시 반영
- [ ] 카드 클릭 → 수정 → 변경 내용 저장
- [ ] 삭제 시 확인 팝업 → 삭제 후 그리드에서 사라짐

#### TC-2.3: 다음 수업 카운트다운 위젯 (홈 화면)

- [ ] 메인 홈(`/`)에 "다음 수업" 큰 위젯 배치
- [ ] 다음 수업까지 **남은 시간 카운트다운** (예: "2시간 13분 남음")
- [ ] 다음 수업 정보: 과목명 + 시작 시간 + 강의실
- [ ] 1분마다 자동 갱신
- [ ] 오늘 모든 수업이 끝났으면 "오늘 수업 끝" 표시
- [ ] 시간표 비어있으면 "시간표를 먼저 입력해 주세요" + 시간표 페이지 링크
- [ ] 주말(토·일)이면 "이번 주 수업 끝" 또는 다음 주 월요일 첫 수업 안내

#### TC-2.4: 오늘 시간표 미니 뷰 (홈 화면)

- [ ] 홈 화면에 **오늘 시간표만 추린 미니 카드 리스트**
- [ ] 각 카드: 시간 + 과목명 + 강의실 (작게)
- [ ] 진행 중인 수업은 시각적 강조 (예: 옅은 배경)
- [ ] 끝난 수업은 색 흐리게(muted)

#### TC-2.5: 시간표 공유 코드

- [ ] `/schedule`에 "**공유 코드 만들기**" 버튼
- [ ] 클릭 시 6자리 영숫자 코드 생성 (예: `K3M9P2`) 후 큰 글씨로 표시
- [ ] 같은 사용자가 다시 누르면 기존 코드 재사용 (한 명당 하나)
- [ ] "복사" 버튼으로 클립보드에 한 번에 복사
- [ ] `/schedule`에 "**친구 코드 입력**" 버튼
- [ ] 코드 입력 → 유효하면 그 사용자의 시간표를 **본인 시간표에 사본으로 복사**
- [ ] 본인 기존 시간표가 있으면 "기존 시간표를 덮어쓸까요?" 확인
- [ ] 잘못된 코드 입력 시 "코드를 찾을 수 없습니다" 안내
- [ ] 복사한 시간표는 **원본과 분리** — 본인이 수정해도 원본에 영향 ❌

#### TC-2.6: 과제 모듈 (`/tasks`)

- [ ] `/tasks` 페이지에 **과제 목록** (마감 임박순)
- [ ] 각 과제 카드: **과목명**(연결된 시간표 과목 또는 자유 입력) · **제목** · **마감일/시간** · **남은 시간 카운트다운** · **라벨**(개인/팀/시험/발표/퀴즈)
- [ ] "과제 추가" 버튼 → 폼 (제목, 마감일, 마감 시간, 과목 선택, 라벨, 메모)
- [ ] 완료 체크박스 → 체크 시 카드가 "완료" 섹션으로 이동, 시각적 muted
- [ ] D-day가 지난 미완료 과제는 빨간 톤 마커 (`#B5483A`)
- [ ] 라벨별 색상 마커 (절제된 톤 — 보라/네온 ❌)
- [ ] 삭제 시 확인 팝업

#### TC-2.7: 임박 과제 위젯 (홈 화면)

- [ ] 홈에 **임박 과제 3건 미리보기**
- [ ] 각 카드: 과목명 + 제목 + D-day
- [ ] "더 보기" 클릭 시 `/tasks` 이동
- [ ] 등록 과제 0건이면 "등록된 과제가 없습니다" + 추가 링크

#### TC-2.8: 데이터 저장 + RLS

- [ ] `schedule_entries` 테이블: 본인만 SELECT/INSERT/UPDATE/DELETE (RLS)
- [ ] `tasks` 테이블: 본인만 SELECT/INSERT/UPDATE/DELETE (RLS)
- [ ] `share_codes` 테이블: 본인 코드는 SELECT/UPDATE/DELETE 가능, 코드 검색은 anon 호출 RPC로만
- [ ] 새로고침해도 데이터 유지

#### TC-2.9: 빌드 + 디자인 검증

- [ ] `npm run build` 에러 0
- [ ] `tsc --noEmit` 에러 0
- [ ] `npm run lint` 경고 0
- [ ] AI 슬롭 패턴 검색 결과 0건 (보라 그라디언트, 네온, 흰 카드+파란 CTA, shadow-2xl 남발 등)
- [ ] 디자인 시스템 컬러 팔레트만 사용 (DailyTodo Warm Paper)

---

#### Sprint 2 데모 시나리오

```
1. 학생 A (이미 가입+승인됨)으로 로그인
2. 시간표 페이지 → 월요일 9:00~10:30 "게임사운드기초" 추가 (강의실 K-201, 색상 brick)
3. 화/수/목/금에도 과목 몇 개 추가
4. 그리드에 카드들이 시간만큼 늘어나서 표시되는지 확인
5. 홈 페이지로 이동 → "다음 수업: 게임사운드기초 / 2시간 13분 남음" 위젯 확인
6. 오늘 시간표 미니 뷰에 오늘 수업들이 시간순으로 보이는지 확인
7. 시간표 페이지 → "공유 코드 만들기" → K3M9P2 같은 코드 발급
8. 시크릿 창에서 학생 B(다른 학번)로 가입+승인까지
9. 학생 B로 로그인 → 시간표 페이지 → "친구 코드 입력" → K3M9P2 → 복사 성공
10. 학생 B 시간표가 학생 A와 동일하게 채워짐 확인
11. 학생 B가 한 과목 수정 → 학생 A 시간표에는 영향 없음 확인
12. 과제 페이지 → "프로그래밍 과제 #1" 마감 3일 후로 추가 → 라벨 "개인 과제"
13. 홈에 임박 과제 위젯에 그 과제 떠있는지 확인
14. 모바일 화면(375px)에서 시간표 가로 스크롤 또는 일별 뷰 정상 동작 확인
```

### Sprint 3: 팀 + 칸반 + 마일스톤
*(Sprint 3 시작 시 작성)*

### Sprint 4: 시각화 대시보드 ⭐
*(Sprint 4 시작 시 작성. 차트별 검증 기준 + 성능 기준 포함 예정)*

### Sprint 5: 게임화 + 친구 + 배포
*(Sprint 5 시작 시 작성)*

---

## 평가 도구

### 자동화 테스트
- **Vitest** — 단위 테스트
- **Playwright** — E2E 테스트 (로그인 시나리오 등)
- **Lighthouse CI** — 성능/PWA 점수

### 수동 검증 체크리스트
- 디자인 검증 (`/impeccable-design` 원칙)
- 모바일/태블릿/데스크톱 반응형
- 실제 사용자 시나리오 (학생 1명에게 테스트 의뢰)

---

**📅 문서 작성일**: 2026-04-27
**📌 다음 문서**: CLAUDE.md (Claude Code 작업 가이드)
